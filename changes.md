For things in intake review, the customer should have an action to cancel their own submission in the event they dont need it anymore

lets start the my submissions grid as empty with no items. have a message saying 'You have not submitted any requests yet'

lets add a column to the grid that shows a scheduled date, it would only be populated with 'review scheduled' is the state

on the new request form, add a text input called ESATS ID. it accepts a number and should have a validate button next to it. mock a validation check, the form can't be submitted until its validated.

i like the tabs on the review queue but each chip (or whatever it is) with the number in that queue is being cut off by the next tab. 

on the TM Team View, lets rename Customer Work tab to Customer Rework

for the ID, lets use the format TMR-YY-NNNN where year is last two digits of year (e.g. 2026 = 26) and NNNN is a sequential number. so the first in the queue for 2026 is TMR-26-0001, 2nd is TMR-26-0002 etc

